import { PieceIcon } from './PieceIcon';

export const Piece = ( {item} ) => {
    return (
            
            <>
            
            <PieceIcon type={item.type} />

            </>

    )
}