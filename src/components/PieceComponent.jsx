import { PieceIcon } from './PieceIcon';

export const PieceComponent = ( {item} ) => {
    return (
            
            <>
            
            <PieceIcon item={item} />

            </>

    )
}